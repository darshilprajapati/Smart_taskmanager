services:
  - type: web
    name: smart-task-manager
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      gunicorn -k eventlet -w 1 run:app

    envVars:
      - key: DATABASE_URL
        sync: false

      - key: SECRET_KEY
        sync: false